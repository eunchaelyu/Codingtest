class Solution {
    public int solution(String s) {
        int answer = 0;
        answer=Integer.parseInt(s); //정수 객체로 리턴
        
        //answer=Integer.valueOf(s); //기본형 정수로 리턴 
        return answer;
    }
}
